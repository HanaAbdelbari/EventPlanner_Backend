-- Create database if it doesn't exist
CREATE DATABASE IF NOT EXISTS eventplanner;

-- Use the database
USE eventplanner;

-- Tables will be auto-created by GORM when backend connects
-- This file just ensures the database exists